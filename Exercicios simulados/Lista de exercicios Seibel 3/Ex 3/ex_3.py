# Fazer programa para verificar se uma string lida da entrada contém um dado caracter.
# O programa deve indicar quantas vezes o caracter ocorre na string (retorna 0 se não ocorrer).
# Deve ainda usar comando de repetição.

texto = input('Escreva seu texto aqui: ')
caracter = input('Qual caractere você que verificar: ')

print(texto.count(caracter))
