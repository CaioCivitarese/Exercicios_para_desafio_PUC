# A. Implemente um jogo de adivinhação. O computador “escolhe” (gera aleatoriamente) um
# número entre 1 e 1023, e o usuário tenta adivinhar o número escolhido. Para cada tentativa do
# usuário, o programa deve exibir na tela:
# • o número -1, se o número gerado for menor do que o número fornecido pelo usuário;
# • o número 1, se o número gerado for maior do que o número fornecido pelo usuário;
# • o número 0, se o número gerado for igual ao fornecido pelo usuário. Neste caso, o programa
# deve exibir o número de tentativas usadas pelo usuário para acertar a escolha do computador e
# finalizar a execução.
# Implemente e teste este jogo.
# B. Em seguida, implemente a variação do jogo na qual usuário e computador podem trocar de
# funções. O programa começa perguntando quem tentará adivinhar: 0 (o usuário) ou 1 (o
# computador). Se a escolha for 0 (o usuário), executa-se o código já desenvolvido. Se a escolha
# for 1 (o computador), entra numa lógica diferente. Agora, o usuário pensa em um número entre
# 1 e 1023 e o computador tenta acertar. Para cada tentativa feita pelo computador, o usuário
# deve entrar com os números -1, 1 ou 0, indicando que o número pensado for menor, maior ou
# igual ao número exibido pelo computador, respectivamente. Naturalmente, as tentativas feitas
# pelo computador devem seguir uma determinada estratégia. Você seria capaz de implementar
# uma estratégia adequada? Saiba que uma boa estratégia garante que o computador precisa de
# no máximo 10 tentativas para acertar o número!
# Organize seu código em funções.
# Quem é melhor neste jogo de adivinhação: você ou o computador?

